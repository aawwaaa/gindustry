# Context
Filename: GDScriptAdapter_Fix_Task.md
Created On: 2024-12-19
Created By: AI Assistant
Associated Protocol: RIPER-5 + Multidimensional + Agent Protocol

# Task Description
修复 GDScriptAdapter 源代码生成器中的问题

# Project Overview
这是一个 Godot 游戏项目，使用 C# 源代码生成器来为标记了 `GDScriptAdapterTarget` 属性的类生成 GDScript 适配器代码。当前生成器存在多个问题需要修复。

---
*The following sections are maintained by the AI during protocol execution*
---

# Analysis (Populated by RESEARCH mode)

## 发现的问题：

1. **属性名称不匹配**: 代码中查找 `"GDScriptAdapterTarget"`，但实际属性是 `GDScriptAdapterTargetAttribute`
2. **空引用风险**: 使用 `attribute.ArgumentList!.Arguments.FirstOrDefault()?.ToString()!` 可能导致运行时错误
3. **Execute 方法为空**: 没有实际的代码生成逻辑
4. **缺少 using 语句**: 没有引用 `Gindustry.Attributes` 命名空间
5. **参数解析错误**: 直接使用 `ToString()` 获取参数值，应该解析为字符串字面量

## 代码结构分析：
- 属性定义在 `src/attributes/GDScriptAdapterTarget.cs`
- 生成器在 `csgen/gdscriptadapter/GDScriptAdapter.cs`
- 多个类使用了该属性，如 `TestRun`, `SubEntity`, `Entity` 等
- 属性接受一个字符串参数作为目标名称

## 技术约束：
- 使用 .NET 8.0
- 启用 Nullable 引用类型
- 使用最新的 C# 语言版本
- 项目构建成功，但生成器功能不完整

# Proposed Solution (Populated by INNOVATE mode)

## 解决方案选择：混合修复方案

选择方案3的原因：
- 快速解决当前问题，最小化风险
- 保持现有架构，减少对项目的影响
- 为未来改进留下空间

## 具体修复策略：

### 1. 属性名称修复
- 将 `"GDScriptAdapterTarget"` 改为 `"GDScriptAdapterTargetAttribute"`
- 或者使用更灵活的匹配策略

### 2. 参数解析改进
- 使用 `LiteralExpressionSyntax` 正确解析字符串参数
- 添加空值检查和错误处理
- 移除危险的 `!` 操作符

### 3. 代码生成实现
- 实现基本的 Execute 方法
- 生成简单的 GDScript 适配器类
- 包含必要的导入和基本结构

### 4. 错误处理增强
- 添加适当的日志记录
- 处理异常情况
- 提供有意义的错误信息

## 技术实现要点：
- 使用 Roslyn 的 `LiteralExpressionSyntax` 进行参数解析
- 实现基本的代码生成模板
- 保持与现有代码风格的兼容性
- 添加适当的注释和文档

# Implementation Plan (Generated by PLAN mode)

## 文件修改计划

**文件**: `csgen/gdscriptadapter/GDScriptAdapter.cs`
**修改原因**: 修复属性名称匹配、参数解析和代码生成逻辑

## 具体修改内容

1. **添加必要的 using 语句**
   - 添加 `using Microsoft.CodeAnalysis.CSharp;` 用于 `LiteralExpressionSyntax`
   - 添加 `using Gindustry.Attributes;` 用于属性引用

2. **修复属性名称匹配**
   - 将 `"GDScriptAdapterTarget"` 改为 `"GDScriptAdapterTargetAttribute"`
   - 或者使用 `EndsWith("GDScriptAdapterTarget")` 进行更灵活的匹配

3. **改进参数解析逻辑**
   - 使用 `LiteralExpressionSyntax` 正确解析字符串参数
   - 添加空值检查和错误处理
   - 移除危险的 `!` 操作符

4. **实现 Execute 方法**
   - 添加基本的代码生成逻辑
   - 生成简单的 GDScript 适配器类
   - 包含必要的导入和基本结构

5. **增强错误处理**
   - 添加适当的日志记录
   - 处理异常情况
   - 提供有意义的错误信息

## 实施检查清单

```
Implementation Checklist:
1. 添加必要的 using 语句到 GDScriptAdapter.cs
2. 修复属性名称匹配逻辑，使用 "GDScriptAdapterTargetAttribute"
3. 改进参数解析，使用 LiteralExpressionSyntax 并添加空值检查
4. 移除危险的 null-forgiving 操作符 (!)
5. 实现基本的 Execute 方法，包含代码生成逻辑
6. 添加错误处理和日志记录
7. 验证构建成功
8. 测试代码生成功能
``` 